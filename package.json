{
  "name": "api_nodejs",
  "version": "1.0.0",
  "description": "Desenvolvimento Inicial API REST NODEJS",
  "main": "dist/entry.js",
  "scripts": {
    "build": "tsc",
    "watch-ts": "tsc -w",
    "dev": "tsc && npm run build  && nodemon -r dotenv/config --inspect ./dist/entry.js",
    "start": "nodemon ./dist/entry.js",
    "prod": "npm run build && npm run start"
  },
  "keywords": [
    "API",
    "REST",
    "NODE"
  ],
  "author": "Thiago de Carlo Goncalves",
  "license": "ISC",
  "devDependencies": {
  },
  "dependencies": {
    "@types/node": "^20.4.1",
    "typescript": "^5.1.6",
    "dotenv": "^16.3.1",
    "nodemon": "^3.0.1"
  }
}
